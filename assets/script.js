$(document).ready(function() {

    let dayEl = $('#currentDay');

    dayEl.text(moment().format('dddd MMMM Do YYYY, h:mm:ss a'));

        //var currentTime = moment();
        //currentTime = currentTime.startOf("hour");

});